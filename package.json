{
  "name": "lambda-config-loader",
  "version": "1.1.10",
  "description": "A simple config loader for AWS lambda functions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "fferr10",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security"
  },
  "devDependencies": {
    "aws-sdk": "^2.368.0"
  }
}
